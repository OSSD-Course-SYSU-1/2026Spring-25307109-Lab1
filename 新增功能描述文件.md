# 第三版新增功能描述文件

> **项目名称**：HarmonyOS 音乐播放器  
> **版本**：第三版（V3）  
> **新增日期**：2026年6月1日  
> **适用产品**：Phone（手机） + Watch（手表）

---

## 一、新增功能总览

| 序号 | 功能名称 | 影响范围 | 新增文件数 |
|:----:|----------|----------|:----------:|
| 1 | **睡眠定时器** | 公共库 + Phone + Watch | 3 个源文件 + 2 个图标 |
| 2 | **随机播放 + 单曲循环** | Phone + Watch 控制区域 | 1 个枚举 + 6 个图标 |
| 3 | **手表端 SongPage 播放页面** | Watch 产品 | 1 个源文件（集成上述功能） |

---

## 二、功能详细描述

### 🕐 功能一：睡眠定时器

#### 功能概述
提供倒计时自动暂停播放功能，支持 15 / 30 / 45 / 60 分钟四档预设选项。超时后自动暂停音乐，适配夜间听歌场景。Phone 和 Watch 端均有独立的 UI 弹窗和入口按钮。

#### 核心服务

| 文件路径 | 说明 |
|----------|------|
| `common/mediacommon/src/main/ets/utils/SleepTimerService.ets` | 单例服务，管理定时器生命周期 |

**关键接口**：
- `startTimer(minutes: number)` — 启动定时器，按分钟设定
- `stopTimer()` — 手动停止定时器
- `getIsActive()` — 查询定时器是否激活
- `getRemainingSeconds()` — 获取剩余秒数
- `getDisplayTime()` — 获取格式化倒计时（MM:SS）

**实现细节**：
- 使用 `setInterval` 每秒轮询更新倒计时
- 倒计时归零时自动调用 `MediaService.getInstance().pause()` 暂停播放
- 状态通过 `AppStorage` 全局同步：
  - `sleepTimerActive: boolean` — 是否激活
  - `sleepTimerRemainingSeconds: number` — 剩余秒数
  - `sleepTimerDisplayTime: string` — 格式化显示时间

#### Phone 端 UI

| 文件路径 | 说明 |
|----------|------|
| `features/musiclist/src/main/ets/components/SleepTimerDialog.ets` | 睡眠定时器弹窗（Phone） |
| `features/musiclist/src/main/ets/components/ControlAreaComponent.ets` | 控制区域（新增入口按钮） |

- **弹窗**：支持 `sm / md / lg` 三档响应式布局
  - 小屏/中屏：纵向列表布局，每项显示"X 分钟后停止播放"
  - 大屏（lg）：2×2 Grid 网格布局，展示分钟数+文字
  - 激活状态：显示倒计时 + "取消定时器"按钮
- **入口按钮**：播放控制栏新增 `ic_sleep_timer` 图标，激活时高亮为选中色并显示剩余时间文字

#### Watch 端 UI

| 文件路径 | 说明 |
|----------|------|
| `products/watch/src/main/ets/view/SleepTimerWatchDialog.ets` | 睡眠定时器弹窗（Watch） |
| `products/watch/src/main/ets/view/SongPage.ets` | 歌曲页（新增入口按钮） |

- **弹窗**：简化版 2×2 Grid 布局，半透明暗色背景，适配圆形表盘
- **入口按钮**：底部功能栏新增 `sleep_timer` 图标，激活时绿色高亮 `#4CAF50` 并显示倒计时数字

---

### 🎵 功能二：播放模式增强（随机播放 + 单曲循环）

#### 功能概述
在原有顺序播放基础上，新增**随机播放**和**单曲循环**两种模式。点击按钮在模式间切换，通过视觉反馈（颜色 + 图标叠加）标识当前激活状态。

#### 模式枚举

| 文件路径 | 说明 |
|----------|------|
| `common/mediacommon/src/main/ets/viewmodel/MusicData.ets` | 新增 `MusicPlayMode` 枚举 |

```typescript
enum MusicPlayMode {
  SINGLE_CYCLE = 0,  // 单曲循环
  ORDER = 1,         // 顺序播放（默认）
  RANDOM = 2         // 随机播放
}
```

#### 随机播放

| 特性 | 说明 |
|------|------|
| 操作 | 点击切换 随机 ↔ 顺序 |
| 激活态颜色 | 淡粉色 `#FFB6C1` |
| 未激活态颜色 | 白色 `#FFFFFF` |
| Phone 图标 | `ic_random_play` |
| Watch 图标 | `random_play` |

#### 单曲循环

| 特性 | 说明 |
|------|------|
| 操作 | 点击切换 单曲循环 ↔ 顺序 |
| UI 实现 | `Stack` 布局，箭头图标上叠加数字 **"1"** |
| 激活态颜色 | 淡粉色 `#FFB6C1` |
| 未激活态颜色 | 白色 `#FFFFFF` |
| Phone 图标 | `ic_repeat_arrow` + 文字 "1" |
| Watch 图标 | `repeat_arrow` + 文字 "1" |

**Phone 端修改文件**：
- `features/musiclist/src/main/ets/components/ControlAreaComponent.ets`
  - 随机播放按钮位于控制栏最左侧
  - 单曲循环按钮位于控制栏最右侧

**Watch 端修改文件**：
- `products/watch/src/main/ets/view/SongPage.ets`
  - 随机播放按钮位于控制栏最左侧
  - 单曲循环按钮位于控制栏最右侧

#### 图标规范

控制区域图标统一使用白色 `#FFFFFF` 填充作为未激活态。激活态颜色规范：
- **随机播放、单曲循环**：激活态使用淡粉色 `#FFB6C1`
- **睡眠定时器**（Phone）：激活态使用主题选中色
- **睡眠定时器**（Watch）：激活态使用绿色 `#4CAF50`

---

### ⌚ 功能三：Watch 端 SongPage 播放页面

#### 功能概述
为手表端新增完整的歌曲播放页面，集成了歌曲信息展示、播放控制栏、睡眠定时器入口和音量调节功能。

#### 文件路径
- `products/watch/src/main/ets/view/SongPage.ets`

#### 页面结构

```
┌─────────────────────────┐
│      歌曲标题            │
│      歌手名称            │
├─────────────────────────┤
│ 随机 │ 上一首 │ 封面 │ 下一首 │ 单循 │
│      │        │(环形) │        │      │
│      │        │ 播放/ │        │      │
│      │        │ 暂停   │        │      │
├─────────────────────────┤
│ 下载 │ 循环 │ 定时 │ 全屏  │
└─────────────────────────┘
        音量滑块 (ArcSlider)
```

#### 页面特性
- 使用 `NavDestination` 作为导航目标，支持返回手势
- 环形进度条（`ProgressType.Ring`）叠加专辑封面
- 渐变背景（`linearGradient`）由底部深色渐变
- 集成 `VolumeSlider` 组件（透过数字表冠控制音量）
- 睡眠定时器按钮激活时显示绿色高亮 + 倒计时数字
- 随机/单曲循环按钮激活时显示粉色高亮 `#FFB6C1`

---

## 三、公共库导出变更

**文件**：`common/mediacommon/index.ets`

新增导出项：
```typescript
export { MusicPlayMode } from './src/main/ets/viewmodel/MusicData';
export { SleepTimerService, SLEEP_TIMER_OPTIONS } from './src/main/ets/utils/SleepTimerService';
```

---

## 四、新增资源文件清单

### Phone 端图标（features/musiclist）
| 文件名 | 用途 |
|--------|------|
| `ic_sleep_timer.svg` | 睡眠定时器按钮图标 |
| `ic_random_play.svg` | 随机播放按钮图标 |
| `ic_repeat_arrow.svg` | 单曲循环按钮图标 |

### Watch 端图标（products/watch）
| 文件名 | 用途 |
|--------|------|
| `sleep_timer.svg` | 睡眠定时器按钮图标 |
| `random_play.svg` | 随机播放按钮图标 |
| `repeat_arrow.svg` | 单曲循环按钮图标 |
| `single_cycle.svg` | 单曲循环标记图标 |

---

## 五、AppStorage 全局状态键

| 键名 | 类型 | 说明 |
|------|------|------|
| `sleepTimerActive` | `boolean` | 睡眠定时器是否激活 |
| `sleepTimerRemainingSeconds` | `number` | 睡眠定时器剩余秒数 |
| `sleepTimerDisplayTime` | `string` | 睡眠定时器格式化显示（MM:SS） |
| `playMode` | `MusicPlayMode` | 当前播放模式 |

---

## 六、技术要点

1. **单例模式**：`SleepTimerService` 采用单例模式，确保全局只有一个定时器实例
2. **AppStorage 跨组件通信**：定时器状态和播放模式通过 `@StorageLink` 实现跨组件实时同步
3. **响应式布局**：`SleepTimerDialog` 使用 `BreakpointSystem` 实现 sm/md/lg 三档自适应
4. **ArkUI 手表专属组件**：Watch 端使用 `ArcList`、`ArcSwiper`、`ArcSlider` 等手表专属组件
5. **图标颜色规范**：未激活态统一白色 `#FFFFFF`，激活态按功能区分不同颜色，确保视觉一致性

---

> **共计**：新增 **7 个源文件**、**7 个 SVG 图标资源**，修改 **3 个现有文件**，新增 **1 个公共库导出**。
